<h2>
    Patients
</h2>
<ul>
    <% @patients.each do |p| %>
        <li> 
            Name: <%= link_to p.name %> 
            Number of Appointments: <%= p.appointments.size %>
        </li>
    <% end %>
</ul>
